# grass

Vite + React 19 + React Three Fiber demo: 72k instanced grass blades on a 3x3 tile grid.

## Structure

- `src/App.jsx` — Canvas, scene composition, ground plane, soil block. Scene switching is a two-target compositor, not a flip: `Scenes` mounts the map hub, grass diorama, `<City>`, and `<AnnArbor>` into four separate `THREE.Scene`s via `createPortal`, but only the active pair renders each frame into two SHARED `useFBO` targets (4x MSAA) — outgoing scene → `outgoingFBO` (skipped entirely when no transition is running), incoming/resident scene → `residentFBO` — so hidden scenes cost no GPU time and new destinations cost no extra VRAM; the default scene contains only a fullscreen `ScreenQuad` with `blendMaterial` compositing the two textures (`uMix`, smoothstepped `p` 0→1 over ~1.6s driven by the `.flip-btn` toggle) — not a crossfade: an fbm-raggedized diagonal front sweeps the frame with heat-ripple UV warp and an additive ember rim at the dissolve edge, and the threshold overshoots by the edge width so both endpoints fully resolve. All scenes stay mounted and animating (JS-side) at all times; StylePass grades the composed quad. Transitions also zoom in 3D: the grass and city portal contents are each wrapped in a scale-animated `group` (`grassRoot`/`cityRoot`, driven in the same `useFrame`) — the incoming diorama dollies up 0.55→1 while the outgoing recedes 1→0.55, paired with MichiganHub's own map dive toward the clicked destination; the sky quads sit outside these roots so they stay fullscreen. Each portal scene also carries a billboarded troika title (drei `<Text>` in `SceneTitle`: "UP NORTH" / "DETROIT") hung below the diorama's front corner at [4.8,-3.4,4.8] (nameplate under the soil block's front tip) — depth-test off so buildings never occlude it, and being in-scene it rides the burn wipe like everything else. Each scene renders its own sky — the meadow uses the shared leva-driven `skyMaterial`, the city a module-level clone with the steely palette baked in — so there's no palette-crossfade bookkeeping. All canvas content sits in one `<Suspense fallback={null}>` with the custom `<MittenLoader />` overlay (`src/MittenLoader.jsx`) outside: the GLB (mounted eagerly) suspends the whole scene until loaded, which is the preload. The loader is a Michigan-themed splash: a fullscreen `#1c4966` veil rect whose SVG mask cuts the Upper/Lower Peninsula shapes out as a window onto the live canvas, capped by an opaque green fill riding the same transform+clip so the loader reads solid while loading (progress fills bottom-up via a clip rect shared by mask hole and cap; "PURE MICHIGAN" label + pct in an absolutely-positioned `.mitten-loader-ui` block below the shape), driven by drei `useProgress` with a 4s minimum: fill = min(elapsed/4s, ceiling) where the ceiling creeps asymptotically from real progress toward 99 (useProgress only reports two coarse asset steps and can sit on one number for seconds on a slow network — the creep keeps the count visibly ticking). Once both assets are loaded, 4s have passed, AND `window.__scenePainted` is set — flipped at the end of Scenes' useFrame on its second frame, i.e. after the frame that ate the shader-compile hitch has actually presented, so the window never opens on a black canvas — the pct swaps for an ENTER button (the click gesture also unlocks audio autoplay); clicking it dissolves the green cap (first ~35% of the reveal — the scene appears inside the shape) while the mask window scales up (easeInOutCubic, ~2.1s, rAF-driven) around an anchor inside the lower peninsula until it swallows the viewport, then unmounts. The map hub is the default opening scene (`#city`/`#meadow` deep-link straight into a diorama); the ENTER click fires the `mitten-done` window event (and sets `window.__mittenDone`) that Camera.jsx and CloudCover.jsx use to start the load intro — a slow cinematic push-in through the map's two-layer cloud blanket as it burns off. AA lives ONLY in the two samples:4 FBOs — the canvas is `antialias: false` and StylePass's composers run `multisampling={0}`, since both only ever draw fullscreen quads; don't re-enable either. Shadow maps are on-demand (`src/shadows.js`): `Scenes` sets `shadowMap.autoUpdate = false` on mount and re-arms `needsUpdate` per render while a transition is animating (captured BEFORE the completion reset so the settle frame gets a final refresh); the other arm sites are MeadowLights mount (App.jsx) and marker hover damp (MichiganHub.jsx — the preview blocks cast and scale). Renders of scenes without shadow lights (city, blend quad) don't consume the flag. Known freeze: the loungers' breathing and leva map tilt/zoom knobs move casters without re-arming — invisible under the 0.1-opacity catcher / dev-only. DOM HUD (`Hud` in App.jsx + `.hud-*` in index.css): a top-left `.hud-michigan` badge (static Michigan silhouette reusing MittenLoader's `UP_PATH`/`MITTEN_PATH`/`MICHIGAN_VIEWBOX`, tinted by the per-scene `--hud-accent`) and a bottom-center morphing island — on the map it holds the three destination buttons (name only, no chapter numbers or brand text), in a scene it shrinks to the back-to-map pill; the old topbar/side-map/footer HUD CSS is deleted.
- `src/city/City.jsx` — downtown Detroit diorama, authored ground-at-y=0, mounted at the origin of its own portal scene. The whole scene is one GLB (`public/detroit.glb`): Tripo-generated buildings (RenCen, stadium, arena, civic tower, brick block) plus baked ground-with-river, roads, sidewalks, park, and trees; Draco-compressed meshes, KTX2 textures. `BakedCity` loads it via drei `useGLTF` (no local Suspense — it suspends up to App's loading screen; Draco via drei's default CDN decoder, KTX2 via a module-singleton `KTX2Loader` pointed at the drei-assets basis CDN and `detectSupport`ed against the renderer), strips the GLB's embedded cameras/light, and swaps every mesh to unlit `MeshBasicMaterial` (lighting is baked into the textures) — except the `Detroit_River_Blue` primitive, which gets a module-singleton animated river `ShaderMaterial` — deliberately calmer than `Ocean.jsx`: dark steely two-tone ripple drifting along the corner diagonal, no crests/sheen/glint/sparkles. It reuses the shared `uTime` uniform object from `grass/material.js`, so Grass's frame loop animates it (Grass is always mounted — both scenes render every frame). The material swap in the traverse also re-points any prior ShaderMaterial (StrictMode double-run / HMR on the cached mutated scene) so the river never falls into the basic-material branch and goes white. Scene is authored 30×30 with ground at y=0, so it mounts at scale 0.5 to fit the 15×15 slab. No light rig; the only other pieces are the unlit slab box under the ground and `<People>` — the people system lives in `src/city/People.jsx` (`makePeople(clumps)` factory + `<People people scene>` component, shared with Ann Arbor; the matrix-recompose loop is a module-level `updatePeople()` so the React Compiler lint doesn't see prop mutation): two InstancedMeshes per city (cylinder bodies with per-instance shirt colors + skin-toned sphere heads, unlit) scattered in hand-picked clumps around each landmark, authored in GLB coords and mounted at the same 0.5 scale; ~70% of them walk small tangent-facing circles around their spawn point with a step bob (per-person params baked at module load), the rest stand. Detroit's clump singleton stays in City.jsx. `Ballfield` repaints the stadium's playing field, whose baked texels are covered in crack/seam artifacts: an opaque static `ShaderMaterial` ellipse (unit `CircleGeometry` scaled to the measured field disc, radii 2.9×2.48 authored) hovering 0.012 above the field surface — level measured by Draco-decoding the stadium mesh in node (flat up-facing disc at local y≈-0.230 × node scale 5.0693 + translation → authored y 0.356, center offset +0.456x) — drawing mow-stripe arcs, dirt basepath diamond (L1 band), mound/home circles, bases, foul lines, warning track, and an edge vignette; home plate points at the +x+z camera-side corner and the rim tucks under the rising stands so no alpha fade is needed. `ComericaPark` makes the stadium clickable: an invisible solid cylinder over the stadium (marker pattern from MichiganHub — handlers gate on `hubTransition.to === 'city'` since the shared event root raycasts hidden portal scenes too, and MichiganHub's marker handlers gate BEFORE `stopPropagation` for the same reason) launches baseballs from home plate toward center field — a module-singleton pool (`MAX_BALLS` slots, oldest recycled) of `public/baseball.glb` clones (Draco+KTX2, loaded via the shared `ktx2` singleton, normalized to `BALL_R` off its bounding sphere, swapped to unlit basic; every click adds a ball) with integrated per-ball physics: spray/power jitter seeded off the launch timestamp (no Math.random), damped bounces, then a friction roll to rest — resting balls persist on the ground until recycled, and balls that clear the tile edge fall past the soil block before despawning. One pool-wide InstancedMesh comet trail (per-ball position ring buffers, shrinking translucent spheres, collapsed to scale 0 when not flying). A "baseball" leva folder writes transiently to the singletons (GLB clones are normalized to unit radius so size is a root-scale write; trail length/width/opacity/color hit `ballParams`/the trail material). `<PeopleMover>` is the Detroit People Mover: a module-singleton closed `CatmullRomCurve3` loop (authored coords, elevated to y=1.3) rendered as an unlit concrete `TubeGeometry` guideway on 36 instanced pillars, with a two-car train in DPM livery (white box body + half-cylinder rounded ends, dark upper window band, grey roof, teal accent stripe, dark bogie skirt riding the tube) whose `useFrame` slides both cars along the curve (~70s/lap) via `getPointAt`/`getTangentAt` + `lookAt` (the lookAt target is local+tangent scaled by the mount's uniform 0.5). The waypoints were verified against the GLB's building AABBs (extracted by parsing detroit.glb in node with a Worker/fetch-shimmed GLTFLoader) — the sampled curve clears every building footprint by ≥0.3 authored units; re-run that check if buildings move.
- `src/AnnArbor.jsx` — Ann Arbor diorama: one Tripo GLB (`public/ann-arbor_compressed.glb`, Draco + KTX2, authored 30×30 / ground y≈-0.075 like Detroit, mounted at scale 0.5), declared `KHR_materials_unlit` so GLTFLoader already yields MeshBasicMaterials — no material swap, just a camera/light strip plus `patchBakedWater`: the Huron River is baked flat teal into the site atlas (no separate water mesh/material like Detroit's), so the site/ground materials get an `onBeforeCompile` that color-keys a water mask (smoothstep on blue−red — every other site color is red-heavy or grey) and remaps masked texels through a Detroit-river-style drifting multi-scale ripple (deep/lit two-tone graded from the baked color, world-xz noise ×2 for authored feature size, shared grass `uTime`) plus a slow lapping wash on the mask's ramp band at the banks; `userData.water` guards the StrictMode/HMR re-traverse of the cached scene. Reuses `CityBase` for the urban-fill slab, the `ktx2` transcoder singleton exported from `City.jsx`, and `makePeople`/`People` from `src/city/People.jsx` (its own clump singleton keyed to the GLB's landmark nodes). A big `MichiganFlag` (exported from MichiganHub.jsx, scale 3.2, yaw π/4 to face the iso camera) stands on the Diag at scene center, using `calmFlagMaterial` — a clone of the flag shader with `uSpeed` 0.42 so the wave stays gentle at that size (uTime/uMap re-pointed at the shared objects after clone) — over a Birds.jsx-style blob contact shadow (radial-gradient CanvasTexture ellipse, neutral dark, elongated along the fly direction; the unlit baked ground can't receive real shadow maps). Wired as scene id `annarbor` (App portal with its own maize-warm sky clone, `annarborRoot` dolly group containing the SAME steel `CloudSheet` singleton as Detroit — shared material and leva folder, so the haze knobs drive both scenes — `#annarbor` deep link, HUD destination button; the portal carries the same `computePortalPointer` events config as map/city/grass — every interactive portal needs it or clicks raycast a stale camera). `BlockM` is the scene's interactive moment: click the Diag flag (invisible cylinder, gated on `hubTransition.to === 'annarbor'`) and the formation driver walks every person to a target on the block M's OUTLINE (sampled from `/m.png`'s alpha at native aspect, bbox-normalized, interior cells dropped — ~125 people can't fill a readable glyph but trace its contour crisply; laid out along the iso camera's screen axes with `M_STRETCH` countering ground-plane foreshortening; people swell 1.3× in formation — the wander radius `wr` and swell `s` ride each person's travel ease alongside `hx`/`hz`, never snap in `sendPeople`, or the whole crowd teleports a frame on click), holds a giant maize block M for 5s (shirt instanceColors chase maize from a saved snapshot; the flag stirs via the amplitude-only `uExcite` uniform (wave amp +45% and a constant-speed flutter, phase-continuous; every click also snaps a decaying `kick` impulse to 1 so the flag whips instantly — click feedback long before the crowd reads); do NOT animate `uSpeed` — wave phase is `uTime * uSpeed`, so ramping it against a large uTime teleports the cloth per frame), then disperses and restores wander — all by animating each person's `hx`/`hz` home point, so the shared `updatePeople` renderer is untouched.
- `src/MichiganHub.jsx` — the map hub scene: extruded UP + mitten geometry (built from `MittenLoader`'s SVG paths), stylized water plane, and three `DestinationMarker`s (Detroit / Up North / Ann Arbor) with mini previews, pulsing rings, floating pins, and labels; marker palettes live in `MARKER_COLORS` keyed by id (Ann Arbor is maize-yellow). Idle markers are deliberately quiet — dim near-static pulse ring, half-opacity label, zero pin emissive, tiny bob (except on touch screens, where hover never fires: labels rest at full opacity and 1.5× scale) — and the damped hover state carries the drama (pin lift/scale, ring pulse waking, label to full, preview growth), so the highlighted destination differs in shape/motion, not just tint; each marker also sits on a Birds.jsx-style radial-gradient contact blob (shared module CanvasTexture) that deepens on hover, and both tree `InstancedMesh`es cast real shadows (static casters, free under the on-demand shadow map). Ann Arbor's preview is `MichiganFlag` (exported — AnnArbor.jsx mounts a big one too; props position/scale/yaw/material): a pole + block-M flag whose ShaderMaterial waves the cloth in the vertex shader off the shared grass `uTime` scaled by a `uSpeed` uniform (hoist pinned, amplitude grows to the fly, wave-slope shading) and composites `/m.png` over the blue field in the fragment (alpha-safe for both full-image and cutout-M PNGs); pole/finial are MeshStandardMaterial (AnnArbor.jsx mounts a flag-only hemisphere+directional rig — its baked materials are all unlit, so the lights shade nothing else) and the flag doesn't cast shadows (on-demand shadow maps would freeze the wave). Map trees (and the Up North preview cones) are single `InstancedMesh`es of one unit cone, uniformly scaled per tree — not per-tree meshes. Labels are billboarded MANUALLY in the marker's `useFrame` (local quaternion = `billboardQuat` (inverse of the static tilt∘y-angle parent rotation, computed in render) × camera quaternion) — drei's `<Billboard>` useFrame never fires inside this FBO portal, so its labels rendered skewed. The water plane is 120×120 so its edges stay off-screen even at the intro's pulled-back zoom on any aspect (the shader keys off plane-local coords and the shore mask clamps to open water, so plane size is free); it's a module-singleton ShaderMaterial (no gridHelper anymore) — a slow-drifting 3-octave fbm samples a two-stop blue gradient (`uWaterA`/`uWaterB`, driven transiently by a "water" leva folder), ripple sheen dialed to a whisper, a faint ~2% survey grid (2.4-unit cells in plane-local coords), very slight vertex bob (96×96 segments), riding the shared grass `uTime` like Ocean/river. Foam appears only along the coastline: `bakeShoreMask()` canvas-bakes a shore-distance proxy (blurred white land fill minus sharp black land fill → rim fading outward, ~0.5 at the waterline) from the same SVG polys, and the fragment darkens the water toward the coast off it (so the cream landmass silhouette pops) then draws a narrow waterline wash + thin low-wobble lapping rings traveling toward land; the plane sits INSIDE the `rotation-y={angle}` group so the mask stays registered to the land. A "map" leva folder drives tilt (quaternion about the (1,0,−1) `TILT_AXIS` diagonal toward the iso camera, on a group outside the animated `mapRoot` so the dive math is untouched), zoom (base scale multiplied into the dive scale in the `useFrame`), and angle (the land group's rotation-y, also used for the dive's focus rotation); each marker's pointer target is a single invisible cylinder (`colorWrite`/`depthWrite` off) sized to the visible marker (preview footprint through label) so hover/click is one solid volume, not per-mesh slivers. The land itself is NOT interactive — markers (and the HUD buttons, synced via `hoverHubDestination`) are the only click targets, and the marker click/hover handlers early-out unless `hubTransition.to === 'map'` (R3F's event raycast hits the invisible marker cylinders even while a diorama is showing — shared camera/event root — which let you jump scene-to-scene without returning to the map); `mapRoot` scales/translates in a `useFrame` to dive toward the clicked destination during scene transitions (the tilt group co-rotates the offset, so at full focus the destination lands at the scene origin).
- `src/CloudCover.jsx` — flat shader stratus sheets (the instanced cumulus `CloudLayer` deck is deleted; `CloudCover`, still mounted in the map portal, now renders only the load-intro blanket). Load intro: `IntroStratus` — TWO stacked warm stratus sheets (`INTRO_LAYERS`, module-singleton `makeCloudSheetMaterial`s, cover 1.15 — past the shader's solid point so they start gapless; 240×110 planes, deeper than the diorama sheets since the intro camera holds a pulled-back zoom, same near edge but the extra depth pushed back along (−1,0,−1)/√2 to stay inside the ortho far box — the higher layer's near edge pushed back a bit further since altitude moves the near-clip crossing up-plane) blanket the map, counter-drifting for parallax; when `window.__mittenDone` flips, the high thin veil clears over ~1.9s and the dense low blanket burns off over ~2.6s (delayed 0.3s, opacity chased out at each tail — a break-through-the-clouds reveal matching Camera.jsx's slow push-in), then both meshes unmount for good; the meadow/city clouds are `CloudSheet`s instead — a single transparent horizontal plane per diorama (inside `grassRoot`/`cityRoot` so they scale with the transition dolly; 170×65, spun 45° about y so its width runs along the iso camera's screen-x and clears both screen edges at any aspect, pushed back so it fits the ortho near/far box — the camera-side uv.y fade dissolves clouds before the near-clip line crossing the layer) whose fragment shader draws wispy stratus: fbm noise rotated into the wind frame and squashed across-wind into long streaks, coverage-thresholded to alpha with a soft rectangular edge fade and a shade tone mixed into denser cores; no volume, no lights, no useFrame — the two module-singleton materials (warm / steel) ride the shared grass `uTime` like Ocean/river. Each sheet has its own leva folder ("stratus · up north" / "stratus · detroit"): all knobs write transiently to that tint's uniforms (cover/opacity/softness/streak scales/speed/direction/colors, initialized from the preset) except altitude, which re-renders the mesh position.
- `tools/blender/` — the (now legacy) bake pipeline that generated the previous Blender-authored detroit.glb: drives Blender through the blender-mcp addon socket (`bmcp.py`), builds the scene in three.js coordinates (`P(x,y,z)→(x,-z,y)`, glTF export converts back), joins into 2 atlas objects, smart-UVs, Cycles-GPU-bakes COMBINED, exports the GLB. The current GLB is Tripo-sourced, not produced by this pipeline.
- `src/ClickHint.jsx` — shared hover affordance for the diorama interactives (Comerica Park and the Diag flag; the skipping-stone water instead lightens its WHOLE band via the ocean shader's damped `uHover` uniform — the water is the click target, so the water is the affordance): `ClickHint` is an additive radial-gradient glow disc (module-singleton CanvasTexture/geometry, per-mount basic material) that softly lightens the clickable area — invisible at idle, damped fade-in on the `hovered` prop, gated per scene; `useClickCursor(on)` sets the pointer cursor by writing the CANVAS's inline style via a module-level setter — drei's `useCursor` writes `document.body`, which loses to `.scene canvas { cursor: grab }`, and the inline write is module-level so the React Compiler lint doesn't see hook-state mutation. MichiganHub's marker hover uses the same hook.
- `src/gpu.js` — weak-GPU detection at module load (throwaway WebGL2 context + `WEBGL_debug_renderer_info`, renderer-string blocklist: software renderers, Intel integrated, Mali/Adreno/PowerVR; no context or an exception also counts as low). Exports `lowGPU`; `?gpu=low` / `?gpu=high` force it for testing. Consumed by App.jsx (FBO `samples` 4→0, canvas `dpr` capped at 1, `shadows` off) and Grass.jsx (default/leva blade count cut to a third — the leva max is untouched, so the slider can restore full density).
- `src/sceneState.js` — module singleton `hubTransition` ({from, to, p}, written by App's Scenes) plus `sceneRendering(name)`: true iff that portal scene renders this frame (it's the transition target, or the source while p<1). Every scene-specific `useFrame` early-outs on it (City's People/PeopleMover, Birds, Scenery's Smoke/Loungers, MichiganHub's markers + dive) so hidden scenes cost no JS either; Grass's uTime driver stays unconditional — it clocks every shared-uniform shader in all scenes.
- `src/StyleEffect.jsx` — the only post-processing: one `postprocessing` Effect with a `uMode` switch behind a "style" leva dropdown (ink / none / anime / outline / b&w) plus line-strength and color-band sliders. All modes share a 3×3 sobel on alpha-weighted luma; `none` unmounts the EffectComposer entirely. (The former "serene" mode — Bloom + chroma wash + depth haze — is deleted; its depth input was always 1.0 under the quad-compositor architecture anyway.)
- `src/Camera.jsx` — orthographic isometric corner view at [12.5,11,12.5] targeting (0,-1,-0.4): the square tile reads as a floating diamond diorama (target below ground keeps the soil block's underside in frame). Aspect-aware `view` half-height with clamps — the max clamp is 8.4 in landscape but opens continuously as aspect drops below 1 (portrait phones), and scene-dependently: the map hub backs out more than the dioramas, but both stay tighter than width-fit — side cropping on phones is accepted to keep content big; portrait also shortens the intro pull-back (`zoomOff` 0.75 vs 0.5) so the dolly stays inside the intro cloud planes' bounds, and the portrait map trucks the framing right via `setViewOffset` on the ortho camera (outside CameraControls' pose, so the setLookAt sites are untouched); OrbitControls constrained to a narrow polar/azimuth window around the corner angle; on touch (`hover: none`) the release restore is disabled and maxZoom widened so pinch zoom persists instead of snapping back. Intro dolly on load: while the MittenLoader runs the camera holds a pulled-back zoom (0.5x of resting, `minZoom` widened via `intro` state); on the loader's `mitten-done` event it pushes straight in to the composed view over ~2.4s — a pure dolly, no azimuth/polar motion — through CloudCover's parting intro blanket. The ortho camera's near plane is −50 (negative near is legal for orthographic): at the intro's pulled-back zoom a 0.1 near plane sliced the map water plane across the lower frame. Driven manually in a `useFrame` (easeInOutCubic on a clamped-dt progress, applied via `zoomTo` with transitions off) — NOT CameraControls' damped transitions, because the loader fade lands right after the city's mount/compile hitch and the multi-second frame delta would snap the transition to the end pose before reveal. `intro` flips off at p=1, restoring the tight interaction clamps. Takes App's `activeScene` as a `scene` prop: on any scene flip it eases the shared camera back to the composed default pose/zoom (same move as the drag-release restore), so a zoomed-in view never carries into the incoming scene. A very slight mouse-parallax yaw (`MOUSE_YAW`, damped in a `useFrame`) lives on a parent `group` around the camera — CameraControls owns the camera's local pose, so the sway never fights it.
- `src/coast.js` — single source of truth for the in-tile coastline: `COAST_EDGE` (grass is fully dune sand by z=-COAST_EDGE), `LAND_EDGE_Z` (ground plane discards past this; the ocean plane below shows through), `WATERLINE_Z`, plus the shared sand/dune-grass colors + uniforms that keep ground, grass, ocean, and soil block sand identical. The grass vertex shader, ground fragment, and SoilBlock all interpolate these constants.
- `src/grass/constants.js` — GRID/TILE/blade counts/param-texture layout constants.
- `src/grass/shaders.js` — GLSL: wind bend + hash-cell clump pull (uClump, cell size from uClumpScale, mound-shaped height taper toward each clump center) + param fetch in vertex, fbm color fields + gust sheen (vGust varying) + AO in fragment. Interpolates constants into the source.
- `src/grass/geometry.js` — `buildGeometry()`: one normalized segmented strip as `InstancedBufferGeometry` with per-blade attributes (offset, scale, rotation, wind phase, tile id). The vertex shader shapes it with live blade-profile uniforms. Blades are laid out blade-major (tiles innermost) so the "blades" slider can truncate `instanceCount` and thin all tiles evenly.
- `src/grass/tileParams.js` — the per-tile `DataTexture` (4 RGBA float texels per tile: colors A/B/C + scales/mixes) and `randomizeTiles()` which rewrites it with hue/sat/lightness-jittered colors. Jitter is seeded (mulberry32) so slider changes re-tint without reshuffling the layout; only `reseed()` (the "randomize" button) rolls a new arrangement.
- `src/grass/material.js` — the `uniforms` object + ShaderMaterial, built manually as module singletons — R3F's `uniforms` prop clones each uniform into the material, so mutating a shared uniforms object via JSX `<shaderMaterial>` never reaches the GPU. Exported so App.jsx's "ground / blade base" control can drive `uBaseColor` too (ground plane and blade roots share one color). Also exports `groundMaterial`: samples the density mask for pressed-turf/soil tint, embosses two-scale noise on the soil for path bumpiness, and reuses the shared `uTime`/`uGustScale`/`uSheen` uniform objects to run the same traveling gust-sheen formula as the blades so the wash lightens the path in sync.
- `src/grass/densityMask.js` — 256² CPU-baked `DataTexture` carving a meandering dirt path: R = surviving blade density, G = pressed-grass amount (also darkens the ground), B = exposed-soil patches. Sampled by both the grass vertex shader (kills/shortens blades) and `groundMaterial` (pressed-turf + soil tint). `setPathParam(key, value)` re-bakes the whole texture (width/soil/clearing/core), driven by the "path" leva folder.
- `src/grass/Grass.jsx` — the mesh + leva controls (`useControls`); color/scale changes and the "randomize" button call `randomizeTiles`. A "wind" folder (strength/speed/gust scale) writes transiently to the `uWindStrength`/`uWindSpeed`/`uGustScale` uniforms via `onChange` — no re-render. A "path" folder mixes both patterns: width/soil/clearing/core call `setPathParam` (CPU mask re-bake), press/soilColor/darken/bump/bumpScale write to uniforms. Path defaults live in `PATH_DEFAULTS` in `defaults.js`.
- `src/rocks/rockMask.js` — 256² world-space occupancy `DataTexture` (`uRockMask` in the grass material), stamped once by `stampSceneryMask(footprints)` (Scenery.jsx, at module load). Writes soft-rimmed circles (solid kill under each footprint, fading out to `RING`× its radius) and the grass vertex shader scales blades down by the mask — grass never grows through the cottage/lounger footprints. (The rock system that originally shared this mask is deleted; its light rig lives on as `MeadowLights` in App.jsx.)
- `src/Ocean.jsx` — Ghibli-style beach + ocean INSIDE the tile along the -z edge (where the dirt path exits): one flat plane exactly as wide as the field, spanning from just inland of `LAND_EDGE_Z` to the tile edge, tucked below the ground plane (the ground discards past `LAND_EDGE_Z`, so the ocean shows through there). Everything painted in the fragment shader keyed off signed distance past a breathing waterline (run-up/retreat + wobble) — mottled dry sand (world-space formula identical to the ground's dune sand, hiding the discard seam) → wet sand with a foam-arc "ghost" → scalloped foam + broken second line → gouache-banded turquoise-to-teal compressed into the short in-tile band. Vertex bob on open water only. Reuses the shared `uTime` uniform object from `grass/material.js`, so Grass's frame loop animates it. Module singleton, no controls. Also hosts `SkippingStones` — click the water (invisible plane over the z ∈ [-FIELD_HALF, WATERLINE_Z] band; handlers gate on `hubTransition.to === 'meadow'`, and App's grass portal carries the same `computePortalPointer` events config as map/city so the raycast uses the live camera): a stone pool (city-baseball pattern) throws flat skippers from beside the camp whose first water contact lands exactly on the click point (velocity solved from flight time), then damped skips → sink; each touch stamps a ring buffer of `(x, z, uTime, amp)` `uRipples` vec4s the ocean fragment shader draws as expanding foam rings + splash cores (water-masked). Stones are Lambert-lit but don't cast shadow (moving casters would freeze under on-demand shadow maps).
- `src/SoilBlock.jsx` — diorama base: a 15×2×15 box under the tile painted entirely in-shader — mottled soil gradient with wavy sediment strata, sparse embedded pebbles plus larger irregular stones (lit top / shaded belly), hanging root tendrils and fine root hairs descending from the lip (faded where the rim is sand), root-shadow band, a ragged grass lip that turns into a deeper sand wash along the coast edges, and the path's sand column carried down the +z face. Unlit; sits below the ground/ocean planes. Module singleton, no controls.
- `src/Scenery.jsx` — set dressing: a gabled cottage on the grass above the dune, stepping stones from its door to the path, and the beach-lounging vignette on the dry-sand band before the waterline — placements in `HOUSE`/`CAMP`/`STONES` are keyed to the in-tile coastline in `coast.js`. The two figures have full limbs (lying figure: one straight leg + one bent knee-up with feet; sitting figure: feet flat on the sand and hands where the propped arms land), distinct skin tones (lying: `skin`, sitting: deeper `skinTan`/`skinTanShadow`), shoulder-joint spheres where the arms meet each torso, swimsuit straps on the lying figure's chest, and a simple face on the sitting figure. The sitting figure's arms/legs are `Limb`/`Segment` joint-point chains — capsules stretched between shared joint coordinates, so segments always connect and pose edits mean moving joint points, not re-deriving capsule transforms; joint spheres are skin-colored (not shadow-toned) on both figures so elbows/knees don't read as mannequin balls. Face details: sitting figure — dark oval eyes, blush dots, nose, mouth — plus a low hair bun poking out behind the head under the hat brim, and a gentle idle `useFrame` in `Loungers` — breathing chest swell/lift on both torsos, the sun hat rocking with the lying figure's breath, and the sitting figure's head (grouped with its hair so they pivot together) slowly scanning the horizon with a breath nod. Cottage build: (gable-end pentagon and chevron roof slab both built as `ExtrudeGeometry` along the ridge — eaves/gable overhang/roof thickness come from the profile, no rotated-box math; ridge-cap cylinder along the peak, timber-framed gable (wall-height beam + raking beams) on the door end, arched extruded door + frame with knob, shuttered windows with mullion cross + sill and a flower box, round gable window, chimney with `useFrame`-animated smoke puffs), stepping stones toward the dirt path, and a beach-lounging vignette near the path exit (striped towel + lying figure with limbs and a straw sun hat over the face, sitting figure with knees-up legs / propped arms / hair cap, leaning parasol with an 8-wedge two-color striped canopy and finial). Plain Lambert materials lit by the `MeadowLights` rig in App.jsx; no controls. Calls `stampSceneryMask` at module load so grass doesn't grow through footprints.
- `src/Bike.jsx` + `src/bike/model.js` — procedural hybrid bike (img2threejs reconstruction of a photo reference) leaning against the cottage's camera-side wall in the meadow scene. `model.js` is pure three (no React): `buildBike()` returns a Group authored in meters (drive side +z, facing +x, tire contact at y=0, ~1.7 long) — capsule frame tubes between shared joint constants (Scenery Limb pattern), torus tire/rim + instanced 28-spoke lattice per wheel, suspension fork, open-torus big chainring + disc rings/cassette, CatmullRom TubeGeometry chain loop, bezier brake/shift cables terminating ON the tube/fork surfaces, scaled-sphere saddle with tail kick, white decal sleeve bands. MeshStandardMaterial zones with deliberately LOW metalness (no envmap in the Lambert-lit meadow — high metalness renders black); every mesh casts shadow (static caster, free under the on-demand shadow map; received by MeadowLights' catcher). Named spin-pivot groups (wheels/crankset/steer) + `root.userData.sculptRuntime`, but nothing animates by design — moving casters would freeze under on-demand shadows. `Bike.jsx` mounts the module-singleton at scale 0.6, world position derived from Scenery's `HOUSE`, leaning −0.17 rad about the tire contact line into the wall; sits inside the house's existing `stampSceneryMask` circle so no new grass-kill stamp. Meshes carry `userData.tier` (0 blockout / 1 structural / 2 form) + `setBikeTier()` — only used by the offline review harness, harmless in-scene.
- `src/Birds.jsx` — 5 low-poly gulls circling above the field (capsule body, sphere head, orange beak, forked shape-geometry tail, two-segment wings with a swept black-tipped outer panel; single white plumage, Lambert-lit like the scenery). Each follows a per-bird Lissajous path whose amplitudes are capped below `FIELD_HALF`, so flight is bounded to the tile by construction — no steering/boids. `useFrame` samples the path at t/t+0.25/t+0.75 for position, heading (`lookAt`), and banked roll into turns; wings beat with a skewed (fast-upstroke) sine, the outer panel trailing the inner in phase, eased into intermittent glides by a smoothstep envelope biased by vertical velocity (beat uphill, glide downhill), with a downstroke body bob. The head (neck-pivot group) idly scans, looks into turns, and tilts down in glides; the tail rudder-yaws against turns, fans up in glide, and flicks with the beat. Each bird drags a blob contact shadow (radial-gradient `CanvasTexture` disc just above the ground, per-bird cloned basic material) that tracks x/z and grows/fades with altitude — the ground/ocean shaders are unlit so real shadow maps can't reach them. Mirrored right wing: only the `scale.x=-1` node's own rotation.z negates — nested rotations keep their sign. Module-singleton geometry/params, no controls.

## Conventions

- Geometry, param texture, and uniforms are module-level singletons — the React Compiler lint forbids render-scope mutation, and there's only one `<Grass>`. Move them into the component if it ever mounts twice.
- `npm run lint` uses the React Compiler eslint rules — strict about purity/mutation during render.
